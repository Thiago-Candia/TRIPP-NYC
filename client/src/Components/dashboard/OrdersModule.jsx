const OrdersModule = () => {
  return (
    <article className="dashboard-card">
      <h2>Orders</h2>
      <p>Base preparada para multi-tienda con `store_id` en modelo `Order`.</p>
      <p>Siguiente paso: endpoints de listado, detalle, estados y timeline de pago/envio.</p>
    </article>
  );
};

export default OrdersModule;
